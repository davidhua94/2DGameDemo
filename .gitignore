# Godot-specific folders and files to ignore
.import/              # Import cache, can be regenerated by Godot
.export/              # Exported builds, not needed for version control
export/               # Directory containing export templates (if used)

# Godot's project-specific settings
*.godot               # Project settings file (contains paths and user-specific data)
*.import              # Import metadata, can be regenerated
.editor_settings-3.*  # User-specific editor settings, don't share

# Binary and compiled files
*.exe
*.dll
*.so
*.dylib
*.pdb

# Backup files
*.tmp
*.bak
*.old
*.save
*.swp

# OS-specific files (MacOS)
.DS_Store
